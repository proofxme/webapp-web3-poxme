'use server';

import React from "react";
import CredentialsList from "app/(dashboard)/credentials/new/credentials-list";

export default async function NewCredential() {
  return (<CredentialsList/>)
}
